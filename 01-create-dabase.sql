--  if you have clash with keywords you can write name using `talently`;
CREATE DATABASE talently;

--  there are main three type of DATABASES
-- Text (enum, text, LONGTEXT, CHAR(char is fixed length), VARCHAR)
-- NUMERIC (INT, SMALLINT, DECIMAL, NUMERIC, FLOAT, REAL )
-- Date (DATE, DATETIME, TIMESTAMP)
-- Boolean (BOOL) 

 
